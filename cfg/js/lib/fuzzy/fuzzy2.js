// library of fuzzy match functions

// stackoverflow.com/questions/9206013/javascript-fuzzy-search
// unamatasanatarai.github.io/FuzzyMatch/test/index.html

var fuzzyMatch = {
	pattern: '',
	prep: function(s) {
		this.pattern = new RegExp('('+s.split('').join(')



( '(' + s.split.join(') +  )
